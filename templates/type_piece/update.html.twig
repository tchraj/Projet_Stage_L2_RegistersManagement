{% extends 'template.html.twig' %}

{% block title %}Modifier une piece
{% endblock %}

{% block body %}
	{{ form_start(PieceForm) }}

	<div class="form-control">
		<div class="row">
			{{ form_row(PieceForm.NomPiece) }}
		</div>
	</div>
	<div class="form-control">
		<div class="row">
			{{ form_row(PieceForm.NumPiece) }}
		</div>
	</div>
		<input type="submit" value="Modifier" class="btn btn-primary">
	{{ form_end(PieceForm) }}

{% endblock %}
