{# {% extends "template.html.twig" %}
{% block body %}
	<a href="#">
		<button class="btn btn-success" data-bs-toggle="modale" data-bs-target="#exampleModal">Créer une visite</button>
	</a>
	<div class="modale fade" id="exampleModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
		<div class="modale-dialog">
			<div class="modale-content">
				<div class="modale-header">
					<h5 class="modale-title" id="exampleModalLabel">Modal title</h5>
					<button type="button" class="btn-close" data-bs-dismiss="modale" aria-label="Close"></button>
				</div>
				<div class="modale-body">
					...
				</div>
				<div class="modale-footer">
					<button type="button" class="btn btn-secondary" data-bs-dismiss="modale">Close</button>
					<button type="button" class="btn btn-primary">Save changes</button>
				</div>
			</div>
		</div>
	</div>
	<style>
		.modale {
			display: none;
			position: fixed;
			top: 0;
			left: 0;
			width: 100%;
			height: 100%;
			background-color: rgba(0, 0, 0, 0.5);
		}
		.modale-content {
			background-color: white;
			width: 80%;
			max-width: 600px;
			margin: 100px auto;
			padding: 20px;
		}
	</style>
	<button id="MontreModalBtn">Afficher le formulaire</button>
	<!-- Div de la modalee -->
	<div id="MonModal" class="modale">
		<div class="modale-content">
			<h2>Formulaire</h2>
			<!-- Votre formulaire ici -->
			<form>
				<!-- ... Champs du formulaire ... -->
				<button id="closeModalBtn">Fermer</button>
			</form>
		</div>
	</div>
	<h1>Liste des visites</h1>
	<div class="overflow-x-auto">
		<div class="flex items-center bg-gray-100 font-sans overflow-hidden">
			<div class="w-full lg:w-5/6">
				<div class="bg-rose-300 shadow-md rounded my-6">
					<table class="min-w-max w-full table-auto">
						<thead>
							<tr class="bg-blue-400 text-sm leading-normal">
								<th class="py-3 px-6 text-left">Date</th>
								<th class="py-3 px-6 text-left">Heure d'arrivée</th>
								<th class="py-3 px-6 text-center">Heure de départ</th>
								<th class="py-3 px-6 text-center">Visiteur</th>
								<th class="py-3 px-6 text-center">Employe visité</th>
								<th class="py-3 px-6 text-center">Etat</th>
								<th class="py-3 px-6 text-center">Motif</th>
								<th class="py-3 px-6 text-center">Actions</th>
							</tr>
						</thead>
						<tbody class="text-gray-600 text-sm font-light">
							{% for visite in visites %}
								<tr class="border-b border-blue-200 hover:bg-gray-100">
									<td class="py-3 px-6 text-left whitespace-nowrap">
										<div class="flex items-center">
											<span class="font-medium">
												{{ visite.DateVisite|date("m/d/Y")}}
											</span>
										</div>
									</td>
									<td class="py-3 px-6 text-left">
										<div class="flex items-center">
											<span>{{visite.HeureDeb}}</span>
										</div>
									</td>
									<td class="py-3 px-6 text-left">
										<div class="flex items-center">
											<span>{{visite.HeureFin}}</span>
										</div>
									</td>
									<td class="py-3 px-6 text-left">
										<div class="flex items-center">
											{% if visite.typeVisiteur == 'Visiteur externe' %}
												{{ visite.visiteurExterne }}
											{% elseif visite.typeVisiteur == 'Employe visiteur' %}
												{{ visite.employeVisiteur}}
											{% endif %}
										</div>
									</td>
									<td class="py-3 px-6 text-left">
										<div class="flex items-center">
											<span>{{visite.EmployeVisite}}</span>
										</div>
									</td>
									<td class="py-3 px-6 text-left">
										<div class="flex items-center">
											<span>{{visite.Motif}}</span>
										</div>
									</td>
									<td class="py-3 px-6 text-left">
										<div class="flex items-center">
											<span>{{visite.EtatVisite}}</span>
										</div>
									</td>
									<td class="py-3 px-6 text-center">
										<a href="#" data-bs-toggle="modale" data-bs-target="#editVisitModal">
											<i class="fas fa-edit"></i>
											<!-- Icône d'édition (vous pouvez utiliser une autre icône) -->
										</a>
									</td>
								</tr>
							{% endfor %}
						</tbody>
					</table>
				</div>
			</div>
		</div>
	</div>
	{#{% for visite in visites %}#}
	{#{% endfor %}
	<script>
		const typeVisiteurField = document.querySelector('#                                        {{ VisiteForm.typeVisiteur.vars.id }}');
const champVisiteurExterne = document.querySelector('#champVisiteurExterne');
const champEmployeVisiteur = document.querySelector('#champEmployeVisiteur');
typeVisiteurField.addEventListener('change', () => {
if (typeVisiteurField.value === 'Visiteur externe') {
champVisiteurExterne.style.display = 'block';
champEmployeVisiteur.style.display = 'none';
} else if (typeVisiteurField.value === 'Employe visiteur') {
champVisiteurExterne.style.display = 'none';
champEmployeVisiteur.style.display = 'block';
} else {
champVisiteurExterne.style.display = 'none';
champEmployeVisiteur.style.display = 'none';
}
});
const MontreModalBtn = document.getElementById('MontreModalBtn');
const closeModalBtn = document.getElementById('closeModalBtn');
const modale = document.getElementById('MonModal');

// Afficher la modalee lors du clic sur le bouton
MontreModalBtn.addEventListener('click', () => {
modale.style.display = 'block';
});

// Fermer la modalee lors du clic sur le bouton de fermeture
closeModalBtn.addEventListener('click', () => {
modale.style.display = 'none';
});
/*
var MonModal = new bootstrap.Modal(document.getElementById('createVisitModal'));
var MonModals = new bootstrap.Modal(document.querySelectorAll('.modale'));*/
	</script>
{% endblock %}
#}
{# {% extends "template.html.twig" %}
{% block body %}
<a href="{{ path('app_add_visite') }}" type="btn btn-success">
					<button type="button">Creer une visite</button>

		</a>
		
<h1>Liste des visites</h1>
	<div class="overflow-x-auto">
		<div class="flex items-center  bg-gray-100 font-sans overflow-hidden">
			<div class="w-full lg:w-5/6">
				<div class="bg-rose-300 shadow-md rounded my-6">
					<table class="min-w-max w-full table-auto">
						<thead>
							<tr class="bg-blue-400  text-sm leading-normal">
								<th class="py-3 px-6 text-left">Date</th>
								<th class="py-3 px-6 text-left">Heure d'arrivée</th>
								<th class="py-3 px-6 text-center">Heure de départ</th>
								<th class="py-3 px-6 text-center">Visiteur</th>
								<th class="py-3 px-6 text-center">Employe visité</th>
								<th class="py-3 px-6 text-center">Etat</th>
								<th class="py-3 px-6 text-center">Motif</th>
								<th class="py-3 px-6 text-center">Actions</th>
							</tr>
						</thead>
						<tbody class="text-gray-600 text-sm font-light">
							{% for visite in visites %}
								<tr class="border-b border-blue-200 hover:bg-gray-100">
									<td class="py-3 px-6 text-left whitespace-nowrap">
										<div class="flex items-center">
											<span class="font-medium">
												{{ visite.DateVisite|date("m/d/Y")}}
											</span>
										</div>
									</td>
									<td class="py-3 px-6 text-left">
										<div class="flex items-center">
											<span>{{visite.HeureDeb}}</span>
										</div>
									</td>
									<td class="py-3 px-6 text-left">
										<div class="flex items-center">
											<span>{{visite.HeureFin}}</span>
										</div>
									</td>		
									<td class="py-3 px-6 text-left">	
										<div class="flex items-center">
											{% if visite.typeVisiteur == 'Visiteur externe' %}
												{{ visite.visiteurExterne }}
											{% elseif visite.typeVisiteur == 'Employe visiteur' %}
												{{ visite.employeVisiteur}}
											{% endif %}
										</div>
									</td>
									<td class="py-3 px-6 text-left">
										<div class="flex items-center">
											<span>{{visite.EmployeVisite}}</span>
										</div>
									</td>
									<td class="py-3 px-6 text-left">
										<div class="flex items-center">
											<span>{{visite.Motif}}</span>
										</div>
									</td>
									<td class="py-3 px-6 text-left">
										<div class="flex items-center">
											<span>{{visite.EtatVisite}}</span>
										</div>
									</td>
									<td class="">
										<div class="flex item-center justify-center">
											<a href="{{ path('app_update_visite',{'id':visite.id}) }}" >
												<div class="w-4 mr-2 transform hover:text-purple-500 hover:scale-110">
													<svg xmlns="http://www.w3.org/2000/svg" fill="none" viewbox="0 0 24 24" stroke="currentColor">
														<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15.232 5.232l3.536 3.536m-2.036-5.036a2.5 2.5 0 113.536 3.536L6.5 21.036H3v-3.572L16.732 3.732z"/>
													</svg>
												</div>
												{#data-bs-toggle="modale" data-bs-target="#editVisitModal{{ visite.id }}"#}
{#</a>
										</div>
									</td>
								</tr>
							{% endfor %}
						</tbody>
					</tbody>
				</table>#}
{# {% for visite in visites %}
    <!-- ... Autres colonnes de la liste ... -->
    <div class="modale fade" id="editVisitModal{{ visite.id }}" tabindex="-1" aria-labelledby="editVisitModalLabel{{ visite.id }}" aria-hidden="true">
        <div class="modale-dialog">
            <div class="modale-content"> 
                <div class="modale-header">
                    <h5 class="modale-title" id="editVisitModalLabel{{ visite.id }}">Modifier la visite</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modale" aria-label="Close"></button>
                </div>
                <div class="modale-body">
                    {% include 'visite/update.html.twig' with {'form': VisiteForm} %}
                </div>
            </div>
        </div>
    </div>
{% endfor %} #}
{# </div>
		</div>
	</div>
</div>
<script>
    const typeVisiteurField = document.querySelector('#{{ VisiteForm.typeVisiteur.vars.id }}');
    const champVisiteurExterne = document.querySelector('#champVisiteurExterne');
    const champEmployeVisiteur = document.querySelector('#champEmployeVisiteur');

    typeVisiteurField.addEventListener('change', () => {
        if (typeVisiteurField.value === 'Visiteur externe') {
            champVisiteurExterne.style.display = 'block';
            champEmployeVisiteur.style.display = 'none';
        } else if (typeVisiteurField.value === 'Employe visiteur') {
            champVisiteurExterne.style.display = 'none';
            champEmployeVisiteur.style.display = 'block';
        } else {
            champVisiteurExterne.style.display = 'none';
            champEmployeVisiteur.style.display = 'none';
        }
    });
var MonModal = new bootstrap.Modal(document.getElementById('createVisitModal'));
    var MonModals = new bootstrap.Modal(document.querySelectorAll('.modale'));

</script>

{% endblock %}
	 
	 {% extends "template.html.twig" %}
	 {% block body %}#}



{# <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Modal Example</title>
    <style>
        /* Styles pour masquer la modale */
        .modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.5);
        }
        .modal-content {
            background-color: white;
            width: 80%;
            max-width: 600px;
            margin: 100px auto;
            padding: 20px;
        }
    </style>
</head>
<body> #}


{% extends "template.html.twig" %}
{% block stylesheets %}
<link href="{{ asset('css/styles.css') }}" rel="stylesheet"/>
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
<link href="https://unpkg.com/tailwindcss@^1.0/dist/tailwind.min.css" rel="stylesheet">
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">		
 <style>
        /* Styles pour masquer la modale */
        .modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.5);
        }
        .modal-content {
            background-color: white;
            width: 80%;
            max-width: 600px;
            margin: 100px auto;
            padding: 20px;
        }
    </style>
{% endblock stylesheets %}
{% block body %}
<h1>Liste des visites</h1>
<div class="overflow-x-auto">
	<div class="flex items-center bg-gray-100 font-sans overflow-hidden">
		<div class="w-full lg:w-5/6">
			<div class="bg-rose-300 shadow-md rounded my-6">
				<table class="min-w-max w-full table-auto">
						<thead>
							<tr class="bg-blue-400  text-sm leading-normal">
								<th class="py-3 px-6 text-left">Date</th>
								<th class="py-3 px-6 text-left">Heure d'arrivée</th>
								<th class="py-3 px-6 text-center">Heure de départ</th>
								<th class="py-3 px-6 text-center">Visiteur</th>
								<th class="py-3 px-6 text-center">Employe visité</th>
								<th class="py-3 px-6 text-center">Etat</th>
								<th class="py-3 px-6 text-center">Motif</th>
								<th class="py-3 px-6 text-center">Actions</th>
							</tr>
						</thead>
						<tbody class="text-gray-600 text-sm font-light">
							{% for visite in visites %}
								<tr class="border-b border-blue-200 hover:bg-gray-100">
									<td class="py-3 px-6 text-left whitespace-nowrap">
										<div class="flex items-center">
											<span class="font-medium">
												{{ visite.DateVisite|date("m/d/Y")}}
											</span>
										</div>
									</td>
									<td class="py-3 px-6 text-left">
										<div class="flex items-center">
											<span>{{visite.HeureDeb}}</span>
										</div>
									</td>
									<td class="py-3 px-6 text-left">
										<div class="flex items-center">
											<span>{{visite.HeureFin}}</span>
										</div>
									</td>		
									<td class="py-3 px-6 text-left">	
										<div class="flex items-center">
											{% if visite.typeVisiteur == 'Visiteur externe' %}
												{{ visite.visiteurExterne }}
											{% elseif visite.typeVisiteur == 'Employe visiteur' %}
												{{ visite.employeVisiteur}}
											{% endif %}
										</div>
									</td>
									<td class="py-3 px-6 text-left">
										<div class="flex items-center">
											<span>{{visite.EmployeVisite}}</span>
										</div>
									</td>
									<td class="py-3 px-6 text-left">
										<div class="flex items-center">
											<span>{{visite.Motif}}</span>
										</div>
									</td>
									<td class="py-3 px-6 text-left">
										<div class="flex items-center">
											<span>{{visite.EtatVisite}}</span>
										</div>
									</td>
									<td class="">
										<div class="flex item-center justify-center">
											<a id="showModalBtn" href="#">
												<div class="w-4 mr-2 transform hover:text-purple-500 hover:scale-110">
													<svg xmlns="http://www.w3.org/2000/svg" fill="none" viewbox="0 0 24 24" stroke="currentColor">
														<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15.232 5.232l3.536 3.536m-2.036-5.036a2.5 2.5 0 113.536 3.536L6.5 21.036H3v-3.572L16.732 3.732z"/>
													</svg>
												</div>
												
											</a>
										</div>
									</td>

									<div id="myModal" class="modal">
										<div class="modal-content">
											<h2>Formulaire</h2>
											<!-- Votre formulaire ici -->
											<form style = "overflow:auto">
												<label>Date</label>
													<input type="date" name="DateVisite" value="{{ visite.DateVisite|date('Y-m-d') }}" />
													<label>Heure de début</label>
													<input type="time" name="HeureDeb" value="{{ visite.HeureDeb }}" />
													<label>Heure de départ</label>
													<input type="time" name="HeureFin" value="{{ visite.HeureFin }}" />
													<label>Motif de la visite</label>
													<input type="text" name="motif" value="{{ visite.motif }}" />
													{#<label>Visiteurs Externes</label>visiteursExternes
													 <select name="visiteurs_externes[]" multiple>
														{% for visiteurExterne in visiteursExternes %}
															<option value="{{ visiteurExterne.id }}" {% if visiteurExterne in visite.visiteursExternes %}selected{% endif %}>
																{{ visiteurExterne.nom }}
															</option>
														{% endfor %}
													</select> #}
													<label>Employé Visité</label>
													<select name="employe_visite[]" multiple>
														{% for EmployeV in employeVisite %}
															<option value="{{ EmployeV.id }}" {% if EmployeV in visite.employeVisite %}selected{% endif %}>
																{{ EmployeV.nom }}
															</option>
														{% endfor %}
													</select>
													<div id="visiteurExterne" class="champ-visiteur">
													<label>Visiteur Externe</label>
													<select name="visiteur_externe[]" multiple>
														{% for visiteurExt in visiteurExterne %}
															<option value="{{ visiteurExt.id }}" {% if visiteurExt in visite.visiteurExterne %}selected{% endif %}>
																{{ visiteurExt.nom }}
															</option>
														{% endfor %}
													</select>
												</div>
												<div id="employeVisiteur" class="champ-visiteur">
													<label>Employé Visiteur</label>
													<select name="employe_visiteur[]" multiple>
														{% for employe in employeVisiteur %}
															<option value="{{ employe.id }}" {% if employe in visite.employeVisiteur %}selected{% endif %}>
																{{ employe.nom }}
															</option>
														{% endfor %}
													</select>
												</div>
													<label>Etat de la visite</label>
													<input type="text" name="HeureDeb" value="{{ visite.EtatVisite }}" />
													<button type="submit"><a href="{{ path('app_update_visite', { 'id': visite.id }) }}">Modifier</a></button>                
												<button id="closeModalBtn" >Fermer</button>
											</form>
										</div>
    								</div>
									
								</tr>
							{% endfor %}
						</tbody>
					</tbody>
				</table>
			</div>
		</div>
	</div>
</div>

    <!-- Bouton pour afficher la modale -->
    {# <button id="showModalBtn" data-bs-toggle="modale" data-bs-target="#myModal">Afficher le formulaire</button> #}

    <!-- Div de la modale -->
    
    <script>
        const showModalBtn = document.getElementById('showModalBtn');
        const closeModalBtn = document.getElementById('closeModalBtn');
        const modal = document.getElementById('myModal');

        // Afficher la modale lors du clic sur le bouton
        showModalBtn.addEventListener('click', () => {
            modal.style.display = 'block';
        });

        // Fermer la modale lors du clic sur le bouton de fermeture
        closeModalBtn.addEventListener('click', () => {
            modal.style.display = 'none';
        });
		const typeVisiteur = document.getElementById('typeVisiteur');
        const visiteurExterne = document.getElementById('visiteurExterne');
        const employeVisiteur = document.getElementById('employeVisiteur');

        typeVisiteur.addEventListener('change', () => {
            if (typeVisiteur.value === 'externe') {
                visiteurExterne.style.display = 'block';
                employeVisiteur.style.display = 'none';
            } else if (typeVisiteur.value === 'employe') {
                visiteurExterne.style.display = 'none';
                employeVisiteur.style.display = 'block';
            }
        });

        // Initial setup based on initial value
        if (typeVisiteur.value === 'externe') {
            visiteurExterne.style.display = 'block';
            employeVisiteur.style.display = 'none';
        } else if (typeVisiteur.value === 'employe') {
            visiteurExterne.style.display = 'none';
            employeVisiteur.style.display = 'block';}
</script>
{% endblock %}
 
